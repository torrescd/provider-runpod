apiVersion: v1
kind: Secret
metadata:
  name: runpod-management
  namespace: runpod-credentials
  labels:
    runpod.crossplane.io/credential-purpose: management
type: Opaque
stringData:
  # Replace through the cluster's encrypted secret-management workflow.
  apiKey: replace-me
---
apiVersion: v1
kind: Secret
metadata:
  name: runpod-inference
  namespace: runpod-system
  labels:
    runpod.crossplane.io/credential-purpose: inference
type: Opaque
stringData:
  # Use an endpoint-scoped inference token, never a management API key.
  apiKey: replace-me
