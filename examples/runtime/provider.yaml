apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-runpod
spec:
  # Replace this non-pullable placeholder with the verified package digest from
  # release-digests.txt. Crossplane runs the runtime embedded in this xpkg.
  package: ghcr.io/torrescd/provider-runpod@sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
  runtimeConfigRef:
    name: provider-runpod-hardened
