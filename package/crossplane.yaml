apiVersion: meta.pkg.crossplane.io/v1alpha1
kind: Provider
metadata:
  name: provider-runpod
  annotations:
    meta.crossplane.io/maintainer: provider-runpod maintainers
    meta.crossplane.io/source: github.com/torrescd/provider-runpod
    meta.crossplane.io/license: Apache-2.0
    meta.crossplane.io/description: |
      A clean-room Crossplane v2 provider for bounded RunPod Serverless
      Templates and Endpoints, with authenticated inference readiness gates.
spec:
  crossplane:
    # v2 namespaced managed resources and safe-start semantics are required.
    version: ">=v2.4.0-0"
  capabilities:
    - safe-start
